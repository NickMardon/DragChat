//react DnD requires that you specify an item type to keep track of where things can be dragged to, otherwise mayhem ensues.

//declaring item types, useful for complicated react dnd hooks.
export const ItemTypes = {
    BADGE: 'badge',
};